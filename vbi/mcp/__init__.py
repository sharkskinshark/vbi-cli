"""MCP server surface for vbi-cli.

Exposes vbi's local-first AI usage data as MCP tools so LLM agents
(Claude Code, Claude Desktop, etc.) can call them directly without
shelling out and parsing CLI text output.
"""
